const logger = require('../../logs')
const sinon = require('sinon')

const { app } = require('../../wirebot')
const {
  expect
} = require('../helpers')

describe('Wireot', () => {
  describe('- Server', () => {
    it('should')
  })
})
